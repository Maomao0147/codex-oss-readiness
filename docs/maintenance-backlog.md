# 建议维护 Backlog

把下面内容复制到 GitHub issues，可以作为第 3 周的真实维护练习。

## Issue 1: Improve README quick start

Type: documentation
Labels: `documentation`, `good first issue`

Goal: Make the quick start easier for first-time Python and GitHub users.

Acceptance criteria:

- README explains how to run the checker locally.
- README links to the first commit guide.
- Commands are tested on Windows PowerShell.

## Issue 2: Add missing-only example

Type: documentation
Labels: `documentation`, `good first issue`

Goal: Show how `--missing-only` helps maintainers focus on incomplete setup tasks.

Acceptance criteria:

- README includes a `--missing-only` example.
- The example explains when to use it.

## Issue 3: Add a repository health check idea list

Type: feature
Labels: `enhancement`, `triaged`

Goal: Prepare the next version of the checker by listing future checks.

Acceptance criteria:

- Add a docs section with future checks such as release notes, test command, and project description.
- No new checker behavior is required in this issue.

## Issue 4: Improve application workbook

Type: documentation
Labels: `documentation`, `triaged`

Goal: Make the application draft easier to customize honestly.

Acceptance criteria:

- Add prompts for project users, maintenance history, and API credit usage.
- Keep the reminder not to exaggerate project usage.

## Triage practice

After copying each issue to GitHub:

1. Add the recommended labels.
2. Add `triaged` once the acceptance criteria are clear.
3. Link the issue from `docs/application-evidence-log.md` after it is public.
